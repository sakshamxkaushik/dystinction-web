import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "DYSTINCTION Technologies - Beyond the Visible Spectrum",
  description:
    "Advanced technology solutions operating beyond conventional boundaries. Stealth-mode B2B technology firm specializing in secure architecture, rapid deployment, and invisible integration.",
  keywords: "technology, security, enterprise, stealth, advanced solutions, B2B",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
